<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/lampada"
        android:label="@string/app_name"
        android:roundIcon="@drawable/lampada"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>
        <activity android:name=".signUpActivity" />
        <activity android:name=".myHomeActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".SmartActivity" />
        <activity android:name=".GoToBed"
            android:theme="@style/Theme.MyApplication.CustomTheme"
            ></activity>
        <activity android:name=".VacationMode"
            android:theme="@style/Theme.MyApplication.CustomTheme"
            ></activity>
        <activity android:name=".AddAction"
            android:theme="@style/Theme.MyApplication.CustomTheme"
            ></activity>

    </application>

</manifest>